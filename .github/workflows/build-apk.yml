name: Build Meta Quest WebXR APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Scaffold Horizon WebXR Native Launcher
        run: |
          mkdir -p app/src/main/java/edu/kent/nursing/controllers
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/mipmap-xxxhdpi

          # App Icon
          curl -s -L "https://jcomi4.github.io/nursingcontrollers/slide1.png" -o app/src/main/res/mipmap-xxxhdpi/ic_launcher.png

          cat << 'EOF' > app/src/main/res/values/strings.xml
          <resources>
              <string name="app_name">KSU Nursing VR Orientation</string>
          </resources>
          EOF

          # Manifest configured for Horizon OS WebXR immersive delegation
          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" android:version="1" />
              
              <application
                  android:allowBackup="true"
                  android:icon="@mipmap/ic_launcher"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
                  
                  <meta-data android:name="com.oculus.supportedPlatform" android:value="quest|quest2|questpro|quest3" />
                  
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:theme="@android:style/Theme.Translucent.NoTitleBar"
                      android:launchMode="singleTask"
                      android:screenOrientation="landscape"
                      android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                          <category android:name="com.oculus.intent.category.VR" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # MainActivity passes an immersive WebXR launch intent directly to the Quest VR compositor
          cat << 'EOF' > app/src/main/java/edu/kent/nursing/controllers/MainActivity.java
          package edu.kent.nursing.controllers;

          import android.app.Activity;
          import android.content.Intent;
          import android.net.Uri;
          import android.os.Bundle;

          public class MainActivity extends Activity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  
                  Uri vrUri = Uri.parse("https://jcomi4.github.io/nursingcontrollers/");
                  Intent intent = new Intent(Intent.ACTION_VIEW, vrUri);
                  
                  // Target Horizon OS internal browser engine specifically for VR presentation
                  intent.setPackage("com.oculus.browser");
                  intent.putExtra("enable_vr", true);
                  intent.putExtra("auto_enter_vr", true);
                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

                  try {
                      startActivity(intent);
                  } catch (Exception e) {
                      Intent fallback = new Intent(Intent.ACTION_VIEW, vrUri);
                      fallback.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                      startActivity(fallback);
                  }
                  finish();
              }
          }
          EOF

          echo "include ':app'" > settings.gradle
          cat << 'EOF' > build.gradle
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath 'com.android.tools.build:gradle:8.2.2'
              }
          }
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          EOF

          cat << 'EOF' > app/build.gradle
          plugins {
              id 'com.android.application'
          }
          android {
              namespace 'edu.kent.nursing.controllers'
              compileSdk 34

              defaultConfig {
                  applicationId "edu.kent.nursing.controllers"
                  minSdk 29
                  targetSdk 34
                  versionCode 3
                  versionName "1.0.2"
              }

              signingConfigs {
                  release {
                      storeFile file("../android.keystore")
                      storePassword "ksunursing"
                      keyAlias "android"
                      keyPassword "ksunursing"
                  }
              }

              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                      minifyEnabled false
                  }
              }
          }
          EOF

      - name: Generate Release Keystore
        run: |
          keytool -genkey -v -keystore android.keystore -alias android \
          -keyalg RSA -keysize 2048 -validity 10000 \
          -storepass ksunursing -keypass ksunursing \
          -dname "CN=KSU, OU=Nursing, O=Kent State University, L=Kent, S=OH, C=US"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build Signed APK with Gradle
        run: gradle assembleRelease
        env:
          ANDROID_HOME: /usr/local/lib/android/sdk

      - name: Calculate SHA256 Hash
        id: hash
        run: |
          cp app/build/outputs/apk/release/app-release.apk ./app-release-signed.apk
          HASH=$(sha256sum app-release-signed.apk | awk '{print $1}')
          echo "SHA256=$HASH" >> $GITHUB_ENV
          echo "========================================================"
          echo "YOUR SHA256 HASH IS: $HASH"
          echo "========================================================"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.2
          name: "KSU Orientation Release v1.0.2"
          body: "SHA256 Hash: ${{ env.SHA256 }}"
          files: app-release-signed.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
